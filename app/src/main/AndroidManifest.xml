<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.laserscorpion.redadalertas"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".adhoctests.TorCheckActivity">
        </activity>
        <activity
            android:name=".adhoctests.JSONTestActivity"
            android:label="@string/title_activity_jsontest"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <!-- <action android:name="android.intent.action.MAIN"/ > -->

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".AlertMapActivity">
        </activity>
        <activity
            android:name=".AlertListActivity"
            android:label="@string/title_activity_alert_list"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <!-- <action android:name="android.intent.action.MAIN"/ > -->

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".adhoctests.AlertCheckerTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>